<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="gov.usgs.wma.gcmrc.mapper.TimeSeriesTranslationMapper">
	<select id="getAqGdawsQualifierMap" parameterType="map" resultType="map">
		SELECT * FROM GDAWS_AQ_QUALIFIER_MAP
	</select>
	
	<select id="getAqGdawsApprovalMap" parameterType="map" resultType="map">
		SELECT * FROM GDAWS_AQ_APPROVAL_MAP
	</select>
	
	<select id="getNetworkHoursOffsetMap" parameterType="map" resultType="map">
		SELECT S.SITE_ID, N.OFFSET_FROM_MST FROM SITE_STAR S, NETWORK N
			WHERE S.NETWORK_NAME = N.NETWORK_NM
	</select>
</mapper>